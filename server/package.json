{
  "name": "avalon-server",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "socket.io": "^2.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
